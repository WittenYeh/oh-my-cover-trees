# CLAUDE.md

## 写作与翻译约定

- 笔记（如 `NOTES.md`）以中文为主。
- **允许中英文混用**：对于学术/技术术语，不必强行把每个词都译成中文。当中文译法生硬、歧义或不如原词清晰时，保留英文原文更好。这不仅适用于单个词，也适用于短语。例如：
  - 单词/术语：generalized / general、expansion constant、doubling dimension、aspect ratio
  - 短语：worst time complexity（不必译成“最坏时间复杂度”）、nearest neighbor search
- 首次出现的关键术语建议给出「中文（English）」对照，之后正文可按通顺程度自由选用中文或英文。
- 数学公式用 `$...$` / `$$...$$`，并避免使用 GitHub 无法渲染的命令（如 `\!`）。
